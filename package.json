{
  "name": "hidden-tests",
  "version": "1.0.0",
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testTimeout": 120000
  },  
  "scripts": {
    "test": "jest --json --outputFile=result.json --silent"
  }
}

