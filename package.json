{
  "name": "hidden-tests",
  "version": "1.0.0",
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testTimeout": 1200000
  },  
  "scripts": {
    "test": "jest --json --outputFile=result.json --silent"
  }
}

